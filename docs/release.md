# Release Procedure

The canonical release procedure lives at the repository root in `RELEASE.md`.

This mdBook page is only an entry point. For the actual release steps, see:

- `RELEASE.md` (repo root)
