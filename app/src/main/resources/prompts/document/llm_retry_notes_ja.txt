前回出力に整合性エラーがありました。以下を必ず修正して、全文を再出力してください。
{{VALIDATION_REASONS}}- メソッド見出し数は {{METHOD_COUNT}} 件に一致させること
- 要注意事項は入力データの列挙項目のみを使用すること
- [推測] は使用しないこと
- 5章（改善提案）は「- なし」と箇条書き1行で記述すること
- 動的解決の未確定情報(verified=false / confidence<1.0)は断定しないこと
- 2章はクラス名/パッケージ/ファイルパス/クラス種別/継承/実装インターフェースを必ず記載すること
- 事前条件は抽象語（適切な値/有効なインスタンス）を避け、条件式で記載すること
- 依存呼び出しに「他」「その他」「等」を使わないこと
- 事前条件に失敗系判定（!x.isSuccess() など）を含めないこと
- 正常フローに early-return / failure / boundary を含めないこと
- 引数なしメソッドの事前条件は原則「なし」とし、反射解決条件は異常・境界または未確定事項へ記載すること
- `x != null` / 「x は null でない」の断定は、ソース上の明示チェックに根拠がある場合のみ記載すること
